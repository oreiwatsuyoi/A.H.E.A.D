'use client';

import AppointmentBooking from '../../src/components/Forms/AppointmentBooking';

export default function AppointmentsPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-50 to-blue-50 py-8">
      <AppointmentBooking />
    </div>
  );
}